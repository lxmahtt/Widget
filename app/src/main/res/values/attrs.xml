<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- HeaderBar自定义属性 -->
    <declare-styleable name="HeaderBar">
        <attr name="isShowBack" format="boolean" />
        <attr name="titleText" format="string" />
        <attr name="rightText" format="string" />
    </declare-styleable>

    <!-- LabelText自定义属性 -->
    <declare-styleable name="LabelText">
        <attr name="labelText" format="string" />
        <attr name="contentText" format="string" />
    </declare-styleable>

    <!--WatchBoard自定义属性-->
    <declare-styleable name="WatchBoard">
        <!--表盘的内边距-->
        <attr name="wb_padding" format="dimension" />
        <!--表盘文字大小-->
        <attr name="wb_text_size" format="dimension" />
        <!--时针的宽度-->
        <attr name="wb_hour_pointer_width" format="dimension" />
        <!--分针的宽度-->
        <attr name="wb_minute_pointer_width" format="dimension" />
        <!--秒针的宽度-->
        <attr name="wb_second_pointer_width" format="dimension" />
        <!--指针圆角值-->
        <attr name="wb_pointer_corner_radius" format="dimension" />
        <!--指针超过中心点的长度-->
        <attr name="wb_pointer_end_length" format="dimension" />
        <!--时刻刻度颜色-->
        <attr name="wb_scale_long_color" format="color" />
        <!--非时刻刻度颜色-->
        <attr name="wb_scale_short_color" format="color" />
        <!--时针颜色-->
        <attr name="wb_hour_pointer_color" format="color" />
        <!--分针颜色-->
        <attr name="wb_minute_pointer_color" format="color" />
        <!--秒针颜色-->
        <attr name="wb_second_pointer_color" format="color" />
    </declare-styleable>

    <!-- verificationCodeView自定义属性-->
    <declare-styleable name="verificationCodeView">
        <!--输入框的数量-->
        <attr name="vcv_et_number" format="integer" />
        <!--输入类型-->
        <attr name="vcv_et_inputType">
            <enum name="number" value="0" />
            <enum name="numberPassword" value="1" />
            <enum name="text" value="2" />
            <enum name="textPassword" value="3" />
        </attr>
        <!--输入框的宽度-->
        <attr name="vcv_et_width" format="dimension|reference" />
        <!--输入框文字颜色-->
        <attr name="vcv_et_text_color" format="color|reference" />
        <!--输入框文字大小-->
        <attr name="vcv_et_text_size" format="dimension|reference" />
        <!--输入框背景-->
        <attr name="vcv_et_bg" format="reference" />
        <!--光标样式-->
        <attr name="vcv_et_cursor" format="reference" />
    </declare-styleable>

    <!--SwitchButton自定义属性-->
    <declare-styleable name="SwitchButton">
        <attr name="switch_shadowRadius" format="reference|dimension" />
        <attr name="switch_shadowOffset" format="reference|dimension" />
        <attr name="switch_shadowColor" format="reference|color" />
        <attr name="switch_uncheckColor" format="reference|color" />
        <attr name="switch_checkedColor" format="reference|color" />
        <attr name="switch_borderWidth" format="reference|dimension" />
        <attr name="switch_checkLineColor" format="reference|color" />
        <attr name="switch_checkLineWidth" format="reference|dimension" />
        <attr name="switch_uncheckCircleColor" format="reference|color" />
        <attr name="switch_uncheckCircleWidth" format="reference|dimension" />
        <attr name="switch_uncheckCircleRadius" format="reference|dimension" />
        <attr name="switch_checked" format="reference|boolean" />
        <attr name="switch_shadowEffect" format="reference|boolean" />
        <attr name="switch_effectDuration" format="reference|integer" />
        <attr name="switch_buttonColor" format="reference|color" />
        <attr name="switch_showIndicator" format="reference|boolean" />
        <attr name="switch_background" format="reference|color" />
        <attr name="switch_enableEffect" format="reference|boolean" />
    </declare-styleable>

    <!--switchButtonDrawable自定义属性-->
    <declare-styleable name="SwitchButtonDrawable">
        <attr name="switch_frameDrawable" format="reference|color" />
        <attr name="switch_stateDrawable" format="reference|color" />
        <attr name="switch_stateMaskDrawable" format="reference|color" />
        <attr name="switch_sliderDrawable" format="reference|color" />
        <attr name="switch_sliderToLeft" format="reference|boolean" />
    </declare-styleable>

    <!--TagGroup自定义属性-->
    <declare-styleable name="TagGroup">
        <!-- Whether the tag group is in append mode. -->
        <attr name="atg_isAppendMode" format="boolean" />
        <!-- If the tag group is in append mode, what the hint of input tag. -->
        <attr name="atg_inputHint" format="string" />
        <!-- The tag view outline border color. -->
        <attr name="atg_borderColor" format="color" />
        <!-- The tag view text color. -->
        <attr name="atg_textColor" format="color" />
        <!-- The tag view background color. -->
        <attr name="atg_backgroundColor" format="color" />
        <!-- The dash outline border color, when in append mode. -->
        <attr name="atg_dashBorderColor" format="color" />
        <!-- The input tag hint text color, when in append mode. -->
        <attr name="atg_inputHintColor" format="color" />
        <!-- The input tag type text color, when in append mode. -->
        <attr name="atg_inputTextColor" format="color" />
        <!-- The checked tag view outline border color. -->
        <attr name="atg_checkedBorderColor" format="color" />
        <!-- The checked text color. -->
        <attr name="atg_checkedTextColor" format="color" />
        <!-- The checked marker color. -->
        <attr name="atg_checkedMarkerColor" format="color" />
        <!-- The checked tag view background color. -->
        <attr name="atg_checkedBackgroundColor" format="color" />
        <!-- The tag view background color, when the tag view is being pressed. -->
        <attr name="atg_pressedBackgroundColor" format="color" />
        <!-- The tag view outline border stroke width. -->
        <attr name="atg_borderStrokeWidth" format="dimension" />
        <!-- The tag view text size. -->
        <attr name="atg_textSize" format="dimension" />
        <attr name="atg_horizontalSpacing" format="dimension" />
        <attr name="atg_verticalSpacing" format="dimension" />
        <attr name="atg_horizontalPadding" format="dimension" />
        <attr name="atg_verticalPadding" format="dimension" />
    </declare-styleable>

    <declare-styleable name="Themes">
        <attr name="tagGroupStyle" format="reference" />
    </declare-styleable>


    <!--可折叠的TextView属性-->
    <declare-styleable name="ExpandableTextView">
        <!-- 允许显示最大行数 -->
        <attr name="maxExpandLines" format="integer"/>
        <!-- 动画执行时间 -->
        <attr name="duration" format="integer"/>
        <!--展开文案-->
        <attr name="expandText" format="string"/>
        <!--收起文案-->
        <attr name="collapseText" format="string"/>
        <!--展开图片-->
        <attr name="expandImage" format="reference"/>
        <!--收起图片-->
        <attr name="collapseImage" format="reference"/>
    </declare-styleable>

</resources>
